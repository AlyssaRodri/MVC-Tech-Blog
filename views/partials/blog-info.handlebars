{{!-- File taken from Group Project 2 --}}

<div class="card mb-6">
  <header class="card-header card-header-title ">
    
        <div class="column" id="blogCardHeader">
            <p class=" has-text-primary-light"></p>
      <a href="/blog/{{id}}"  class="has-text-black is-capitalized is-pulled-left">{{title}} </a></p>
      </div>

      <div class="column" id="blogCardHeader">
      <span class=""> </span>
      </div>
    
  </header>

    <div class="card-content">
        <div>
             <img class="image" src="{{image_url}} ">
        </div>
        <div class="content has-text-justified">
            {{blog_content}}
        </div>
            <div class="is-pulled-right">
                <b>{{user.username}}</b> |    
                 {{format_date created_at}} | <a href="/blog/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
            </div>
        
    </div>